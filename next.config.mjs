/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["media.valorant-api.com"], // Adicione o domínio das imagens aqui
      },
};

export default nextConfig;
